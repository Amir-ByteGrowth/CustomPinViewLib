<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CustomUnderLinePinView" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

        <item name="pinViewStyle">@style/PinWidget.PinView</item>
    </style>

    <style name="Theme.CustomUnderLinePinView" parent="Base.Theme.CustomUnderLinePinView" />

    <style name="PinWidget" />

    <style name="PinWidget.PinView" parent="Widget.AppCompat.EditText">
        <item name="android:background">@null</item>
        <item name="android:minHeight">0dp</item>
        <item name="android:maxLines">1</item>
        <item name="android:textCursorDrawable">@drawable/invisible_image</item>
        <item name="android:textSelectHandle">@drawable/invisible_image</item>
        <item name="android:textSelectHandleLeft">@drawable/invisible_image</item>
        <item name="android:textSelectHandleRight">@drawable/invisible_image</item>
    </style>
</resources>